import styled from 'styled-components';

const Middle = styled.div`
  width: 100%;
  background-color: #191414; /* Spotify Black */
  color: #FFFFFF;
  position: relative; /* 자식 요소들의 기준점 */
  overflow: hidden; /* 배경 원들이 튀어나가지 않도록 설정 */
`;

export default Middle;